{
  "name": "earnit-kids-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor shell for the EarnIt Kids web app",
  "scripts": {
    "sync": "npm run sync:prod",
    "sync:prod": "(cd ../web && npm run build:static) && cp capacitor.config.prod.json capacitor.config.json && npx cap sync",
    "sync:local": "(cd ../web && npm run build:static) && cp capacitor.config.local.json capacitor.config.json && npx cap sync",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android",
    "reverse": "adb reverse tcp:3000 tcp:3000",
    "start:web": "(cd ../web && npm run start)",
    "update": "npx cap update"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1",
    "@capacitor/push-notifications": "^8.1.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1"
  },
  "overrides": {
    "@xmldom/xmldom": "0.8.13"
  }
}
