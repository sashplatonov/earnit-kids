services:
  coins-kids-shop:
    build: .
    container_name: coins-kids-shop
    #ports:
    #  - "65501:3000"
    volumes:
      - ./data.json:/app/data.json
    restart: unless-stopped
    networks:
      - proxy
networks:
  proxy:
    external: true