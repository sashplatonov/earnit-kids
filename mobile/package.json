{
  "name": "coins-kids-shop-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor shell for the Coins Kids Shop web app",
  "scripts": {
    "sync": "npm run sync:prod",
    "sync:prod": "cp capacitor.config.prod.json capacitor.config.json && npx cap sync",
    "sync:local": "cp capacitor.config.local.json capacitor.config.json && npx cap sync",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android",
    "reverse": "adb reverse tcp:3001 tcp:3001",
    "update": "npx cap update"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.8",
    "@capacitor/core": "^5.7.8",
    "@capacitor/ios": "^5.7.8",
    "@capacitor/push-notifications": "^5.0.8"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.8"
  }
}
